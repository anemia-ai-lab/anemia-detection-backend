# Informe de experimento (baseline CNN)

**Run ID:** `20260420T043804Z`  
**Marca de tiempo (UTC):** `2026-04-20T04:38:04.805211+00:00`  
**model_version:** `baseline-mobilenetv2-v1`  
**Semilla aleatoria (reproducibilidad):** `42`

**Versiones de software:**
- TensorFlow: `2.21.0`
- Keras: `3.14.0`

**Callbacks (configuración):**
  - `ModelCheckpoint`: {'class': 'ModelCheckpoint', 'filepath': 'ml/artifacts/models/baseline_mobilenetv2_best.keras', 'monitor': 'val_auc', 'mode': 'max', 'save_best_only': True, 'verbose': 1}
  - `EarlyStopping`: {'class': 'EarlyStopping', 'monitor': 'val_auc', 'patience': 5, 'restore_best_weights': True, 'mode': 'max', 'verbose': 1}
  - `ReduceLROnPlateau`: {'class': 'ReduceLROnPlateau', 'monitor': 'val_auc', 'factor': 0.3, 'patience': 3, 'mode': 'max', 'verbose': 1}

**Umbrales de decisión (evaluación en test):**
- **Umbral teórico de referencia (sigmoid):** **0.5** — no se usa como corte operacional.
- **Umbral operacional (predicciones / interpretación):** **0.231260** (ROC, máximo índice de Youden; J = 0.545619).
- El **umbral operacional** (por defecto el de máximo índice de Youden en el conjunto de evaluación) es el que se documenta como referencia para **interpretación clínica y predicciones** en este informe; el umbral 0.5 se mantiene solo como **referencia teórica** habitual en modelos binarios.

**Muestreo / pesos (resumen del experimento):**
- **`class_weight` en `model.fit`:** **desactivado** (`--no-class-weight`).
- **Oversampling en train (datos):** **sí** (solicitado por CLI: sí).
- Oversampling de positivos en train del `fit` (~1:1); **`class_weight` desactivado** en `fit` (`--no-class-weight`).

**Entrenamiento en dos fases (MobileNetV2):**
- **Fine-tuning parcial del backbone (fase 2):** **habilitado** (épocas fase 2 según CLI: 3).
- **Fase 1 — cabezal (backbone congelado):** **5** épocas, LR **0.001**; subcapas en `mobilenet_backbone`: **154** (ImageNet **no** se actualiza en esta fase).
- **Fase 2 — fine-tuning parcial:** **3** épocas, LR **1e-05**; subcapas backbone **154**: congeladas **124**, entrenables **30**; `--fine-tune-freeze-up-to-layer` = **-30** (primera subcapa entrenable: índice **124**).

**Conteos por clase (splits):**
- **Entrenamiento (subconjunto fit, efectivo para `fit`)** — clase 0 (negative): 398; clase 1 (positive): 398
- **Validación (subconjunto fit)** — clase 0 (negative): 100; clase 1 (positive): 20
- **Test (carpeta `data/test`)** — clase 0 (negative): 123; clase 1 (positive): 27

- **Entrenamiento antes de oversampling (conteos usados para `class_weight`)** — clase 0 (negative): 398; clase 1 (positive): 82

**Oversampling (solo subconjunto train del `fit`, no val/test):**
- **Solicitado (`--oversample-positive`):** sí.
- **Aplicado a los datos:** **sí**
- **Distribución ANTES (conteos por clase, negative/positive):** n₀=398, n₁=82
- **Distribución DESPUÉS:** n₀=398, n₁=398
- **Duplicados positivos añadidos:** 316
- **Método:** Oversampling aleatorio con reemplazo de la clase positiva (label=1) hasta aproximadamente 1:1 con negativos; solo en el subconjunto train del split interno.
- **Nota:** `class_weight` estaba **desactivado** en `fit` (`--no-class-weight`); los conteos antes/después del oversampling siguen documentados para trazabilidad.

## a) Preparación del dataset

Imágenes recortadas a 224×224 a partir del dataset Nature, usando las regiones definidas en la columna NAIL_BOUNDING_BOXES del CSV de metadatos (script `ml/scripts/prepare_nature_dataset.py`). Cada PNG corresponde a un crop de uña.

- **Pacientes (estudio Nature, únicos):** 250
  - Fuente del conteo: metadata.csv (PATIENT_ID únicos)
- **Total de muestras (crops) en disco:** 750
  - Train (carpeta): 600
  - Test (carpeta): 150
- **Estrategia train/validation:** Partición estratificada aleatoria sobre las imágenes de `train/` (validation_split=0.2, semilla=42); las mismas carpetas de clase que usa Keras, orden alfabético de subcarpetas.
- **Estrategia train/test (alto nivel):** Train vs test a nivel de **paciente** en la preparación del dataset: `prepare_nature_dataset.py` asigna todos los crops de un PATIENT_ID solo a train o solo a test (80/20 por defecto). Las carpetas `data/train` y `data/test` reflejan ese reparto; la validación durante `fit` es un subconjunto interno de `data/train`.

## b) Distribución de clases (crops en carpetas)

- **Negative:** 621
- **Positive:** 129

## c) Desbalance de clases

Se calcularon pesos balanceados n/(K·n_c) con K=2 a partir de los conteos del subconjunto train tras el split (**solo referencia**). **No** se pasaron a `model.fit` (`--no-class-weight`); el entrenamiento usó pesos de muestra uniformes implícitos en Keras.

- **`class_weight` en `model.fit`:** **desactivado** (`--no-class-weight`).
- **Oversampling en train (datos):** **sí** (solicitado CLI: sí).

Oversampling de positivos en train del `fit` (~1:1); **`class_weight` desactivado** en `fit` (`--no-class-weight`).

Conteos en el subconjunto **train** tras el split (originales; **antes** de oversampling si aplica). Con `--no-class-weight`, no se usaron para ponderar el loss en `fit`, pero se documentan para trazabilidad:

- Clase 0 (negative): 398
- Clase 1 (positive): 82

**`class_weight` no aplicado en `fit`** (`--no-class-weight`). Pesos balanceados de **referencia** (lo que se habría usado por defecto):

- Referencia clase 0: **0.6030150753768844**
- Referencia clase 1: **2.926829268292683**

## d) Configuración de entrenamiento

- **Modelo:** MobileNetV2 (ImageNet) + GAP + Dropout + Dense(1, sigmoid)
- **model_version (artefacto):** `baseline-mobilenetv2-v1`
- **Tamaño de entrada:** [224, 224]
- **Fase 1 — épocas cabezal (solo cabezal):** 5
- **Fase 2 — épocas fine-tuning backbone:** 3
- **Learning rate cabezal (fase 1):** 0.001
- **Learning rate fine-tuning (fase 2):** 1e-05
- **Augmentación (solo train):** RandomRotation (±≤15°, factor=0.0417 respecto a 2π), RandomZoom (-0.05, 0.05), RandomBrightness (max_delta=0.08), RandomContrast (factor=0.08); solo en train, sin augment en val/test.
- **Semilla:** 42
- **validation_split (interno):** 0.2
- **`class_weight` en entrenamiento:** desactivado (`--no-class-weight`)
- **Oversampling (`--oversample-positive`):** solicitado
- **Fine-tuning parcial del backbone (fase 2):** **habilitado** (`--fine-tune-epochs`: 3).
- **Subcapas MobileNetV2 (fase 2):** total **154**; congeladas **124**; entrenables **30**.
- **Corte de congelación:** `--fine-tune-freeze-up-to-layer` = **-30** (LR fase 2: **1e-05**).

## e) Resultados

- **Conjunto de evaluación:** Carpeta test independiente: ml/data/test

### Interpretación clínica de umbrales

- **Por qué 0.5 no es el umbral operacional por defecto:** Con datos clínicos **desbalanceados** (pocos positivos frente a negativos), fijar 0.5 sobre la salida sigmoide suele **no** coincidir con un punto razonable de la curva ROC: puede inflar la exactitud global mientras **deja sin detectar** demasiados positivos reales. El coste de un falso negativo (pasar anemia por alto) y el de un falso positivo no son simétricos, por lo que el corte 0.5 no debe tomarse como decisión operativa por defecto.
- **Por qué se prioriza el recall (sensibilidad):** En un contexto de **cribado** orientado a detectar anemia, se prioriza la **sensibilidad (recall)** sobre la mera precisión global: captar la mayoría de casos positivos reduce riesgo clínico; los falsos positivos suelen poder aclararse con hemograma u otras pruebas. El índice de Youden en ROC equilibra TPR y FPR y ofrece un umbral de trabajo más informativo que 0.5 bajo desbalance.
- **Uso del umbral operacional:** El **umbral operacional** (por defecto el de máximo índice de Youden en el conjunto de evaluación) es el que se documenta como referencia para **interpretación clínica y predicciones** en este informe; el umbral 0.5 se mantiene solo como **referencia teórica** habitual en modelos binarios.

| Métrica global | Valor |
|----------------|-------|
| Loss (test) | 0.387015 |
| **AUC (ROC, independiente del umbral)** | 0.795092 |

### Métricas principales (umbral operacional = ROC-Youden)

Estas son las métricas **destacadas** para el contexto de tesis / uso clínico documentado; corresponden al corte **operacional** (máximo J en ROC sobre test).

- **Umbral operacional τ:** `0.231260`  
- **Índice de Youden J:** 0.545619

| **Precision** | **Recall (sensibilidad)** | Accuracy |
|---------------|---------------------------|----------|
| **0.454545** | **0.740741** | 0.793333 |

### Referencia: umbral teórico 0.5 (sigmoid)

Solo comparación bibliográfica; **no** se recomienda como decisión principal con datos desbalanceados.

| Accuracy | Precision | Recall (sensibilidad) |
|----------|-----------|------------------------|
| 0.846667 | 0.700000 | 0.259259 |

### Comparación con baseline (`--baseline-experiment-json`)

- **JSON baseline:** `ml/artifacts/runs/experiment_20260420T042800Z.json`
- **run_id baseline:** `20260420T042800Z`

- **Perfil del run baseline (desde JSON):**
  - Épocas **cabezal** / **fine-tuning backbone:** 5 / 0
  - Fine-tuning del backbone en baseline: **no**
  - Oversampling (`--oversample-positive` / aplicado en datos): True / True
  - `class_weight` en `fit` (baseline): **no** (`no_class_weight` en JSON: True)

| Métrica | Baseline | Este run | Δ (este − baseline) |
|---------|----------|----------|------------------------|
| AUC | 0.786510 | 0.795092 | 0.008582 |
| Loss | 0.449831 | 0.387015 | -0.062816 |
| Recall @operacional (Youden) | 0.703704 | 0.740741 | 0.037037 |
| Precision @operacional (Youden) | 0.422222 | 0.454545 | 0.032323 |
| Accuracy @operacional | 0.773333 | 0.793333 | 0.020000 |
| Recall @0.5 (ref.) | 0.370370 | 0.259259 | -0.111111 |
| Precision @0.5 (ref.) | 0.500000 | 0.700000 | 0.200000 |
| Accuracy @0.5 (ref.) | 0.820000 | 0.846667 | 0.026667 |

_Δ > 0 en AUC/recall suele indicar mejora respecto al baseline; en loss depende del signo (menor loss es mejor)._

_En experimentos de fine-tuning, suele cargarse como baseline el JSON del mejor run «oversampling + sin `class_weight`» **sin** segunda fase de backbone, para ver si la fase 2 mejora AUC y recall @operacional._

## f) Artefacto del modelo

- **Ruta .keras:** `ml/artifacts/models/baseline_mobilenetv2.keras`

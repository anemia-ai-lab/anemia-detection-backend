[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
# Imports tras ajustar sys.path para ``baseline`` / ``backend`` desde el subpaquete ``ml/``.
"ml/explainability/gradcam.py" = ["E402"]
"ml/preprocessing/pipeline.py" = ["E402"]